import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function CommandCentrePage() {
  return (
    <PhasePlaceholder
      surface="Command Centre"
      headline="What is happening now?"
      phase="Phase 7"
      description="Live operations, active jobs, today's revenue, costs and contribution, miles and empty miles, what needs attention, and what opportunities exist."
    />
  );
}
