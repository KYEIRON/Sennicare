import { PhasePlaceholder } from '@/components/phase-placeholder';

export default function DriverTodayPage() {
  return (
    <PhasePlaceholder
      surface="Driver App"
      headline="Good morning, Moh"
      phase="Phase 4"
      description="Today's jobs, the current job, what is next, the vehicle, miles and expenses today — with large touch controls, camera capture and proof of delivery."
    />
  );
}
